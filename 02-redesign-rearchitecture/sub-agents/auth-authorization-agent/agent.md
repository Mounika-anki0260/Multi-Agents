---
name: Auth Authorization Agent
id: auth-authorization-agent
role: sub-agent
parentAgent: 02-redesign-rearchitecture-documents
step: 11
description: Define identity provider, authentication, authorization, roles, policies, token/session flow, secrets, and compliance controls.
triggers:
  - auth-authorization-agent
capabilities:
  - step-11
  - auth-authorization
---

# Auth Authorization Agent

## Purpose

Define identity provider, authentication, authorization, roles, policies, token/session flow, secrets, and compliance controls.

## Parent Flow

This sub-agent belongs to 02-redesign-rearchitecture-documents and runs as step 11 in that flow.

## Primary Output

Auth and authorization model

---
name: Entity Model Agent
id: entity-model-agent
role: sub-agent
parentAgent: 03-backend
step: 6
description: Create entities, domain models, persistence models, enums, value objects, and relationships.
triggers:
  - entity-model-agent
capabilities:
  - step-6
  - entity-model
---

# Entity Model Agent

## Purpose

Create entities, domain models, persistence models, enums, value objects, and relationships.

## Parent Flow

This sub-agent belongs to 03-backend and runs as step 6 in that flow.

## Primary Output

Entity/model layer
